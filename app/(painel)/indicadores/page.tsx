import { ComingSoon } from "@/components/painel/coming-soon";

export default function IndicadoresPage() {
  return (
    <ComingSoon
      title="Indicadores"
      description="Faturamento por nível/período, conversão e produtividade entram quando o Supabase estiver conectado — o Dashboard já mostra uma prévia com dados de exemplo."
    />
  );
}
