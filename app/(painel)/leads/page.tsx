import { ComingSoon } from "@/components/painel/coming-soon";

export default function LeadsPage() {
  return (
    <ComingSoon
      title="Leads"
      description="Funil de conversão (indicação de obstetra vs. outros canais) entra quando o Supabase estiver conectado — o formulário de contato do site já envia para /api/leads."
    />
  );
}
